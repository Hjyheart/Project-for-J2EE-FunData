<div class="ui cards">
    <div class="card" ng-repeat="course in myMoocs">
        <div class="content">
            <div class="header">{{course.course_name}} </div>
            <div class="author">{{course.course_owner_name}} </div>
            <div class="description">{{course.course_des}} </div>
        </div>
        <div class="extra content">
            <div class="ui three buttons">
                <div class="ui basic green button" ng-click="moocToDetail(course)">More</div>
                <div class="ui basic blue button" ng-click="moocManager(course)">Manager</div>
                <div class="ui basic red button" ng-click="moocShowWarning(course)">Delete</div>
            </div>
        </div>
    </div>
</div>